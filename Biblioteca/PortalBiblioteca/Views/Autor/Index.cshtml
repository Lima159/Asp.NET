@using PortalBiblioteca.Models

@{
    ViewData["Title"] = "Index";
}
@{
    var autores = ViewBag.Message;
}
<h1 align="center" style="color:white;">Autores</h1>

<a asp-action="Create">Nova Entrada</a>

<table>
    <tr>
        <th>Id</th>
        <th>Nome</th>
        <th>Ações</th>
    </tr>
    @for (int i = 0; i < autores.Count; i++)
    {
        <tr>
            <td>@autores[i].Id</td>
            <td>@autores[i].Nome</td>
            <td>
                <a asp-action="Update" asp-route-id="@autores[i].Id">Edit</a> |
                <a asp-action="Detail" asp-route-id="@autores[i].Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@autores[i].Id">Delete</a>
            </td>
        </tr>
    }
</table>